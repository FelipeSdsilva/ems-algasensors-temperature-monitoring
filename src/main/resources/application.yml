server:
  port: ${SERVER_PORT:8080}

spring:
  jpa:
    open-in-view: false

  application.name: temperature-monitoring

  profiles:
    active: ${APP_PROFILE:dev}

  rabbitmq:
    host: ${HOST}
    port: ${PORT}
    username: ${USER}
    password: ${PASSWORD}